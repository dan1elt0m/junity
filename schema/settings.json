{
  "title": "Junity",
  "type": "object",
  "description": "Settings for the Junity extension.",
  "properties": {
    "hostUrl": {
      "default": "http://localhost:8080",
      "description": "The host URL for the Unity Catalog API.",
      "title": "Unity Catalog Host URL",
      "type": "string"
    },
    "accessToken": {
      "description": "The access token for the Unity Catalog API. Only required if not using Google authentication.",
      "title": "Unity Catalog Access Token",
      "type": "string"
    },
    "googleAuthEnabled": {
      "default": false,
      "description": "Whether to use Google authentication.",
      "title": "Google Auth Enabled",
      "type": "boolean"
    },
    "googleClientId": {
      "default": "not-used",
      "description": "The Google client ID.",
      "title": "Google Client ID",
      "type": "string"
    }
  }
}
